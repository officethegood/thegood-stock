# Phase 0.5 — QR Print (cross-cutting) Design

**Status:** APPROVED — ready for impl
**Date:** 2026-05-19
**Scope:** Add QR code printing to existing scan-target entities (cross-cutting; not its own deploy phase but versioned as Phase 0.5 because it touches Phase 0 + 1 + 4 + 5 surfaces).

## 1. Purpose

ทุก phase 1/2/3/4/5/6 มีการ scan QR/barcode แต่ไม่มี phase ไหน generate/print QR เลย → ผู้ใช้ต้องไปใช้ external QR generator แล้วพิมพ์เอง = เสี่ยง typo + flow แตก. Phase 0.5 ปิดช่องนี้ด้วย shared component + dedicated Staff-accessible print page.

## 2. Scope — 4 entity types

| Entity | Source | QR payload |
|---|---|---|
| **stock_items** | Phase 1 | `sku` (e.g. `SUP-GAUZE-001`) — barcode field ถ้ามีจะ render เพิ่ม |
| **locations** (room/cabinet/shelf/ambulance) | Phase 0 | `code` (e.g. `ROOM-A`, `CAB-A-1`, `AMB-TG1`) |
| **locations type='bag'** (ALS bags) | Phase 4 | `code` (e.g. `BAG-ALS-001`) — same path as locations |
| **oxygen_tanks** | Phase 5 | `serial` (e.g. `OXY-001`) |

**ไม่ทำใน Phase 0.5:** lots, loans, movements, individual linens (no physical artifact or supplier prints lot labels)

## 3. QR payload format

**Decision: Bare code** (per PM Q-QR-1 2026-05-19)

QR string = bare entity code: `SUP-GAUZE-001` / `ROOM-A` / `BAG-ALS-001` / `OXY-001`

Rationale: html5-qrcode scanner reads bare text → existing `searchByBarcode .ilike` matches without modification. External apps (phone camera/Line/Google Lens) show text + copy — user opens our app manually. Trade-off accepted: no deep-link convenience, but zero scanner.js changes.

**ไม่ใช้ URL format** (`https://.../staff-scan.html?code=...`) — เพราะ scanner ปัจจุบันไม่ได้ extract `?code=` param + QR ใหญ่ขึ้นโดยไม่จำเป็น

## 4. Print modes

### 4.1 A4 grid bulk (default)
- Layout: 6 cols × 8 rows = **48 stickers/page**
- Sticker size: 32mm × 32mm (matches A4 label-paper standard, e.g. AAS A4-48 = 0.21 บาท/ดวง)
- Cell content:
  ```
  ┌──────────────────┐
  │   QR (26×26mm)   │
  │                  │
  │ SUP-GAUZE-001    │ ← code, 7pt Roboto Mono
  │ ผ้าก๊อซ          │ ← name, 6pt Sarabun, truncate 24 chars
  └──────────────────┘
  ```
- Multi-select checkboxes on print page → "พิมพ์ที่เลือก (N)" CTA

### 4.2 Single sticker (toggle)
- Sizes: **38mm / 50mm / 76mm** (common Brother QL / Phomemo M02 label sizes)
- One page per sticker, paper size matches
- QR auto-scales to fit minus 4mm margin
- Same content as A4 grid cell but bigger fonts

### 4.3 Print mechanism
- `window.print()` with `@media print` CSS
- A4 grid: `@page { size: A4; margin: 0 }` + flex grid
- Single: `@page { size: <Nmm Nmm>; margin: 0 }`
- Mobile printer flow: Save as PDF → user opens in printer app (Phomemo/Brother)

### 4.4 PNG download fallback (added 2026-05-19, decision Q-QR-6)

**Problem:** iOS Safari has `window.print()` but the system print panel does not integrate with web-rendered content in PWA mode — the dialog opens but produces a blank page. Desktop without a printer gives an empty print preview. Users in both cases need an alternative output.

**Solution:** Auto-detect iOS UA and offer a Print / Download PNG choice modal before calling `window.print()`.

**Detection heuristic:**
```js
const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent);
```

**Fallback routing logic:**

| Platform | Behaviour |
|---|---|
| iOS (any) | Always show Print/PNG choice modal |
| Non-iOS, no saved pref | Go straight to `window.print()` (safe desktop default) |
| Non-iOS, pref='print' | Go straight to `window.print()` |
| Non-iOS, pref='png' | Call `QRPrint.downloadPNG` / `QRPrint.downloadBulkPNG` directly |

Pref stored in `localStorage.qr_print_mode_pref` = `'print'` | `'png'`. Modal has a "จำการเลือกนี้" checkbox. Clearing localStorage resets to default.

**PNG generation — single (`QRPrint.downloadPNG`):**
- Offscreen canvas 1024×1024 px, white background.
- QR rendered via `new QRCode(tempDiv, { width: 768, height: 768 })` into a hidden div; canvas/img element drawn onto main canvas.
- Code label: `bold 36pt monospace` centred below QR.
- Subtitle: `24pt sans-serif` centred below label (max 32 chars).
- Download: `canvas.toBlob → createObjectURL → <a>.click() → revokeObjectURL`.
- Filename: `qr-{code}.png` (non-alphanumeric replaced with `_`).

**PNG generation — bulk (`QRPrint.downloadBulkPNG`):**
- Canvas: 2480 × max(3508, content-height) px.
- 6-column grid, 380×380 px cells, 8px gap, 40px page padding.
- Per cell: 1px dashed `#ccc` border; QR 260×260; label 18pt mono; subtitle 12pt sans.
- Filename: `qr-bulk-{YYYYMMDD-HHmm}.png`.

## 5. Components

### 5.1 New files
| File | Purpose |
|---|---|
| `vendor/qrcode.min.js` | qrcode.js v1.5.x vendored (5KB, MIT) — NO CDN |
| `shared/qr-print.js` | Exports `window.QRPrint` API |
| `staff-print.html` | Standalone page; Employee + Admin access; 4 sub-tabs (สินค้า / สถานที่ / ALS Bags / ถังออกซิเจน) |
| `js/staff-print.js` | Page logic: list + filter + multi-select + print |

### 5.2 Edits
| File | Change |
|---|---|
| `staff.html` | Add CTA "🖨️ พิมพ์ QR" → `staff-print.html` (อยู่ข้าง "สแกนเบิก-จ่าย") |
| `js/locations.js` | Add row-level "🖨️" icon button (Admin path) |
| `js/inventory.js` | Add print button in item modal + bulk-select on items list (Admin path) |
| `shared/styles.css` | Add `@media print` rules + `.qr-sticker-grid` 6×8 layout |
| `sw.js` | Bump CACHE_VERSION; add vendor/qrcode.min.js + shared/qr-print.js + staff-print.html + js/staff-print.js to STATIC_ASSETS |

### 5.3 Future-proof (no work needed; pattern documented)
- Phase 4 ALS Bags tab — เพิ่ม print button column (1 บรรทัด `QRPrint.single(bag.code, ...)`)
- Phase 5 Oxygen tab — เพิ่ม print button column (1 บรรทัด `QRPrint.single(tank.serial, ...)`)
- Phase 6 linens — ใช้ตอนพิมพ์ cabinet QR (= location print path already exists)

## 6. API contract

```js
window.QRPrint.single(code, {
  size: '38mm' | '50mm' | '76mm',
  label: string,           // human-readable code under QR (= code by default)
  subtitle: string,        // optional Thai name under code
  entityType: 'item' | 'location' | 'bag' | 'tank'  // for analytics, not in QR payload
})

window.QRPrint.bulk(rows, {
  // rows: Array<{code, label?, subtitle?}>
  // No size option — always A4 grid 6×8
})

// Added Phase 0.5 PNG fallback (Q-QR-6):

window.QRPrint.downloadPNG(code, {
  label: string,    // default = code
  subtitle: string  // optional
})
// Downloads qr-{code}.png  (1024×1024 px, QR + label + subtitle)

window.QRPrint.downloadBulkPNG(rows, {
  // rows: Array<{code, label?, subtitle?}>
})
// Downloads qr-bulk-{YYYYMMDD-HHmm}.png  (A4 proportioned canvas, 6-col grid)
```

`QRPrint.single` and `QRPrint.bulk` internally call `_resolvePrintMode()` which may show the iOS choice modal (§4.4) before proceeding to `window.print()` or the corresponding download method. `downloadPNG` and `downloadBulkPNG` skip the modal entirely — they are the download path.

## 7. Access control

| Role | Pages accessible | Entities can print |
|---|---|---|
| **Admin** | All admin tabs + staff-print.html | items, locations, bags, tanks |
| **Employee** | staff.html + staff-print.html + staff-scan.html + staff-oxygen.html | items, locations, bags, tanks (read-only data; print fully allowed) |

Implementation: `staff-print.html` uses `requireRole(['Admin','Employee'])` from shared/auth.js (already in Phase 0). Data fetched via REST → existing RLS allows SELECT on all 4 tables for authenticated.

**Hide sensitive fields in Employee view:** Employee sees code + name only. NOT qty, threshold, reorder_threshold, supplier, recalled_reason, current_pressure_psi. Admin can see all (but print sticker shows same minimal content for both — for sticker purpose).

## 8. Decisions log

| ID | Decision |
|---|---|
| Q-QR-1 | QR payload format = **bare code** (not URL deep-link) |
| Q-QR-2 | Sticker sizes = **both** (A4 grid 6×8 + single 38/50/76mm toggle) |
| Q-QR-3 | Employee print access = **all 4 entity types** |
| Q-QR-4 | qrcode.js library = **vendored** (not CDN, offline-first) |
| Q-QR-5 | Phase 4 + 5 print buttons = **deferred to those phases' impl** (Phase 0.5 ships shared component; rows added later) |
| Q-QR-6 | PNG fallback: **always offered on iOS; user-chosen pref on non-iOS** — iOS `window.print()` silently fails in PWA context; PNG is downloadable/shareable via AirDrop or Photos; desktop default stays `window.print()` since printer availability is the norm; pref persisted in `localStorage.qr_print_mode_pref` |

## 9. Out of scope

- Print preview before opening dialog (browser print dialog has its own preview)
- Custom sticker templates beyond size selector
- Print history / audit log (just operations)
- Phase 6 per-linen-piece tags (Phase 6.1)
- QR scanning analytics
- Cloudinary photo on sticker

## 10. Acceptance tests T173-T180

- **T173**: Login as Admin → Locations tab → row "ROOM-A" → click "🖨️" → print dialog opens with single 38mm sticker preview
- **T174**: Login as Admin → Inventory tab → bulk select 3 items → "พิมพ์ที่เลือก" → A4 grid preview shows 3 stickers
- **T175**: Login as Employee Pt1 → staff.html → click "🖨️ พิมพ์ QR" → land on staff-print.html
- **T176**: Employee staff-print.html → tab "สินค้า" → search "ผ้า" → autocomplete shows item → multi-select → print
- **T177**: Employee staff-print.html → tab "สถานที่" → "ROOM-A" → single 50mm → print preview shows QR readable
- **T178**: QR payload verification — `QRPrint.single('TEST-001', {})` → QR scanned with html5-qrcode → returns string "TEST-001" exactly
- **T179**: Cache: open DevTools → Cache Storage → `thegood-stock-v0.4.1` (or v0.6.0 if Phase 5/4/6 bumped) → vendor/qrcode.min.js + shared/qr-print.js + staff-print.html + js/staff-print.js cached
- **T180**: Print dialog opens on Chrome Android mobile (paramedic phone) — confirm `window.print()` works on Android Chrome

## 11. Effort estimate

1 fullstack-developer agent run: ~10-15 min. No migrations. Single git commit + push. Single tag `phase0.5-qr-print`.

## 12. Open question for PM (non-blocking)

- **CACHE_VERSION final value** — depends on Phase 5/4/6 bumping order. If Phase 0.5 lands after P5 (v0.5.0), then v0.5.1. If after P5+P4 (v0.6.0), then v0.6.1. Impl agent picks the next minor based on git state at impl time.

---

**Spec self-review:** ✅ no placeholders / no TBDs / scope tight / internally consistent / no contradiction with Phase 1-6 specs. Ready for impl.

## 11.1 Phase 0.5.1 polish (2026-05-19)

Changes applied as Phase 0.5.1 after audit:

- **Row-level print buttons — Phase 4 ALS Bags (`js/bags.js`):** Each bag card now contains a `🖨️` button (`btn btn-sm btn-link text-stock-accent bag-print-btn`). Click stops card-level event propagation; calls `QRPrint.single(bag.code, { size:'38mm', label: bag.code, subtitle: bag.name || template_name, entityType:'bag' })`. Fallback alert if `window.QRPrint` not loaded. Implements spec §5.3 deferred work.

- **Row-level print buttons — Phase 5 Oxygen Tanks (`js/oxygen.js`):** Tank list table gains a 7th empty-header column; each row renders a `🖨️` button (`btn btn-sm btn-link text-stock-accent oxy-print-btn`). Click stops row-level event propagation; calls `QRPrint.single(tank.serial, { size:'38mm', label: tank.serial, subtitle: tank_size + ' • ' + location_code, entityType:'tank' })`. Fallback alert if `window.QRPrint` not loaded. Implements spec §5.3 deferred work.

- **Phase 5 oxygen trigger search_path tightened (`supabase/migrations/20260519050700_tighten_oxygen_triggers_search_path.sql`):** Audit finding S-4 — mirrors Phase 1.1 fix (20260519020000). `enforce_oxygen_state_machine` and `apply_oxygen_movement` changed from `public, pg_temp` to `public`. `check_oxygen_refill_batch` changed from `public, net, pg_temp` to `public, net` (net retained for `net.http_post`). Function bodies verbatim from 20260519050500.

- **T183 regression test added (`docs/test-checklist.md`):** Cross-page shared module load coverage — catches agent-driven script tag gaps across staff-scan.html, staff-oxygen.html, staff-print.html, admin.html.
